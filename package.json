{
	"scripts": {
		"test": "vitest run",
		"clean": "rimraf --glob dist/*",
		"build": "pnpm run clean && tsc -b ./tsconfig.build.json && copy appsscript.json dist\\ && copy src\\*.html dist\\",
		"push": "pnpm run test && pnpm run build && pnpm clasp push"
	},
	"devDependencies": {
		"@google/clasp": "3.0.6-alpha",
		"@types/google-apps-script": "^2.0.8",
		"@vitest/coverage-v8": "3.2.4",
		"globals": "^16.5.0",
		"rimraf": "^6.1.2",
		"tgas-local": "^1.1.1",
		"typescript": "^5.9.3",
		"typescript-plugin-tgas-local": "^0.0.3",
		"vitest": "^3.2.4"
	}
}
